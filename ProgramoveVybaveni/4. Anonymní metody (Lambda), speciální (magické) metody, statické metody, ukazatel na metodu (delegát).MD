# [Anonymní metody (Lambda), speciální (magické) metody, statické metody, ukazatel na metodu (delegát)](https://youtu.be/tNwfoEaFF68?si=YaftN8RaOIRkU47k)

## O čem mluvit?
- Anonymní metoda (Lambda)
  - anonymní metoda
  - syntax
  - pouzivani treba na hledani v poli
- Ukazatel na metodu (Delegát)
  - Ukazatel na metodu
  - Typy delegátů v C# - (Func, Action, Predicate)
  - Využití -> Callback, Event
- Magické metody (Dunder)
# DODELAT


## Anonymní metody
- Blok kódu, který je nadefinován beze jména
- Může být použit jako proměná

```python
soucet = lambda a,b: a + b
print(soucet(5,1)) # Output : 6

cisla = [1,2,3,4,5,6]
suda_cisla = list(filter(lambda x: x % 0 == 0, cisla))
print(suda_cisla) # Output : 2,4,6
```

## Lambda
- Zkrácený zápis anonymní metody
- Kus kódu, který na vstup dostane proměnou a další vrátí
  - Nemusí při tom volat metodu, oni sami jsou jim podobné
- Nemají jméno
- Mohou být použity jako proměná
- Lambda má určité pravidla -> Nemohou uvnitř obsahovat proměnné a cykly
- Lambda je v C# řešená pomocí knihovny Linq

#### Proč Lambdu použít?
- Můžu funkce posílat jiné funkci jako proměnou
- Pomáhají filtrovat a získavat data z kolekce bez zbytečně dlouhého kódu
- Jsou převážně krátké a jednoduché
- Můžeme díky nim splnit určitý interface

#### Lambda výraz se dělí na 2 typy
1. Expression Lambda
   - Tvořená inputem a výrazem
   - Python:
   ```python
     # Lambda metoda
     add = lambda x, y : x + y
     add(1,2) # -> 3
  
     # Výběr čísel > 2 z listu
     mujList = [1,2,3,4,5]
     filtred = list(filter(lambda x : x > 2, mujList)) # -> [3,4,5]
     ```
1. Statement lambda
    - Tvoření inputem a více příkazy které budou vykonány
   - Python
    ```python
   # Python
   greet = lambda name: print(f"Hello {name}!)
   greet("Honzo") # "Hello Honzo!"
   
#### Volání funkce
```csharp
greet("Honzo") # Vytiskneme "Hello Honzo!"
```

- Další příklad: lambda metody pro sečtení dvou čísel
```python
count = lambda a,b: a+b
print(count(1,2))
```

## Delegát
- Deklaruje si vlastní typ anonymních funkcí
- Proměnná, která odkazuje na nějakou metodu
  - Technicky je to referenční datový typ
- Pro vytvoření se používá **delegát**
  - Při deklaraci mu určujeme jaké metody do něj můžeme ukládat a jaké bude mít vstupní a výstupní hodnoty

#### Využití delegátů
- Callbacky
- Eventy

C#:
```python
def pozdrav(jmeno):
    return f"Ahoj {jmeno}"

def zpracuj_uziv(jmeno, callback_func):
    print("Načítáme data")
    vysledek = callback_func(jmeno)
    print(vysledek)

zpracuj_uziv("Honza", pozdrav)
```

#### Generické delegáty
- V C# máme předdefinované delegáty, takže většinu času není třeba definovat
- Nemusí mít žádný vstup (0-16)
- 1. výstup

#### Predicate
- Bere jeden vstupní parametr
- vrací true / false


#### Action
- Nemá výstup, tzv. delegát co vrací void

## Dunder methods
- `__init__` = konstruktor
- `__str__` = ToString
- `__add__(self,other)` = Scitani
# Sepsány v dokumentaci 3.3 pod `special methods`
```python
class Penize:
    def __init__(self,castka):
        self.castka = castka

    def __str__(self):
        return f"{self.castka}KC"

    def __add__(self, jiny_objekt):
        nova_castka = self.castka + jiny_objekt.castka
        return Penize(nova_castka)

vyplata = Penize(30000)
premie = Penize(5000)

celkem = vyplata + premie
print(celkem)
```

## Statické metody
- Běžné metody ve třídách vždy vyžadují parametr self
- Statické metody patří celé třídě
- Definuje se pomocí dekorátoru @staticmethod viz. dokumentace abc
- Nemá parametr self
- Používá se pro pomocné utility